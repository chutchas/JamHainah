-- ============================================================
-- 0012 — เลิกเก็บพิกัดของผู้ใช้
--
-- พิกัดถูกเก็บไว้เพื่อให้ลิงก์ "…ใกล้ฉัน" ค้นรอบตัวเขา
-- แต่มันบอกได้แค่ว่าเขา "เคย" อยู่ตรงไหน ไม่ใช่ตอนนี้อยู่ตรงไหน
-- คนที่ต้องไปต่อภาษี/ทำบัตร คือคนที่กำลังเดินทาง —
-- เก็บตอนอยู่กรุงเทพ แล้วกดตอนอยู่ชลบุรี ก็พาไปผิดจังหวัด
--
-- ตอนนี้ลิงก์ใช้แบบ ?api=1&query= ซึ่ง Google Maps ใช้ GPS ของเครื่อง
-- ณ วินาทีที่กด ตรงกับคำว่า "ใกล้ฉัน" จริง ๆ และไม่ต้องเก็บอะไรเลย
--
-- ข้อมูลตำแหน่งที่เก็บไว้โดยไม่ได้ใช้ คือภาระตาม PDPA เปล่า ๆ
-- ============================================================
drop view if exists public.v_user_areas;

alter table public.users
  drop column if exists area_label,
  drop column if exists area_lat,
  drop column if exists area_lng,
  drop column if exists area_shared_at;
